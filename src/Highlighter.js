import React from 'react';

const Highlighter= (props) => {
    return (
      <div className="highlighter">
        <p> Brandi's Component {props.startOffset}</p>
      </div>
    );
}

export default Highlighter;
